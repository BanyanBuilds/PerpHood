name: Leverage X V55 Build Gate

on:
  push:
    branches: [main, "build/**"]
  pull_request:

concurrency:
  group: leveragex-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Run Leverage X V55 terminal guards
        run: npm run test:v55-fast
      - name: Build the Next.js application
        run: npm run build
